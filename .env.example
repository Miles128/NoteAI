# NoteAI 环境变量配置
# 复制此文件为 .env 并填入实际值
# 也可以通过应用界面配置，环境变量优先级更高

# LLM API 配置
NOTEAI_API_KEY=
NOTEAI_API_BASE=https://api.openai.com/v1
NOTEAI_MODEL_NAME=gpt-4
NOTEAI_TEMPERATURE=0.7
NOTEAI_MAX_TOKENS=32000
NOTEAI_MAX_CONTEXT=128000

# 工作区路径（留空则通过界面选择）
NOTEAI_WORKSPACE_PATH=
