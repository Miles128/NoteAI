# 2026-06-29

## NoteAI 启动
- 依赖就绪：`uv sync --extra dev --extra rag`（189 packages resolved, 153 checked）
- managed node 24 损坏（`../lib/cli.js` not found），改用 homebrew node 26（`/opt/homebrew/bin/node`）
- 启动命令：`PATH="/opt/homebrew/bin:$PATH" .venv/bin/python run.py`
- 编译成功，GUI 正常运行

## RAG 索引修复
- **问题**：zvec LOCK 文件未在崩溃后自动清理（0 字节残留），导致每次启动报"索引文件被占用"
- **修复**：`python/sidecar/rag/index.py` 新增 `_remove_stale_lock()`，在 `_open_or_create_collection` 锁冲突时自动恢复：
  - 0 字节 LOCK → 直接删除
  - 非空 LOCK → 检查 PID 是否存活，不存活则删除
  - 兜底：强制删除后重试
- 手动清理了当前工作区的 stale LOCK 和 error_state.json
- **需重启 app 让修复生效**
