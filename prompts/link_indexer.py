"""双向链接索引引擎使用的 LLM 提示词。

YAML 版本（prompts/yaml/link_indexer.yaml）优先加载，本文件作为 fallback。
占位变量通过 .format() 填充，字面花括号需双写 {{ }}。
"""

CROSS_REF_LLM_PROMPT = """你是知识库双向链接编辑。源文章：
《{title}》
摘要：{summary}

从下列候选中选出**所有**与源文章有实质关联的笔记（概念重叠、补充、前置知识均可）。数量不设下限或上限，只排除弱相关。
返回 JSON 数组，每项 {{"id": 编号, "reason": "10字内原因"}}。只返回 JSON。

候选：
{candidates}"""

LINK_PAIR_JUDGE_PROMPT = """以下是为每对文章编号的列表。请判断每对文章之间是否存在内容重叠或相似之处。

判断标准：
- 两篇文章讨论相同的主题、技术、工具或概念
- 一篇文章的内容是另一篇的补充、延伸或前置知识
- 两篇文章有实质性的交叉引用价值

请返回 JSON 数组，仅包含有关联的对：
[{{"pair": [1, 2], "reason": "简述关联原因（10字以内）"}}, ...]

如果某对没有关联，不要包含在结果中。只返回 JSON 数组，不要其他文字。

{candidates}"""
