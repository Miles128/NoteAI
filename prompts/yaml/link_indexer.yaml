CROSS_REF_LLM_PROMPT: |-
  你是知识库双向链接编辑。源文章：
  《{title}》
  摘要：{summary}

  从下列候选中选出与源文章有**实质性**关联的笔记（概念高度重叠、互为补充、直接前置知识）。
  宁缺毋滥：只有你能给出明确具体关联理由的才选；仅同属一个主题目录、泛泛相关、或只是
  提到同一个大词（如“AI”“大模型”）的不选。数量不限，但弱相关一律排除。
  返回 JSON 数组，每项 {{"id": 编号, "reason": "10字内具体原因"}}。只返回 JSON。

  候选：
  {candidates}
